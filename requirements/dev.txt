# Test tooling on top of the api runtime. The hosted model's tests stay
# importable without torch/transformers -- conftest.py stubs them when absent.
-r api.txt
pytest==9.0.3
pytest-django==4.12.0
boto3-stubs[s3,sqs]==1.43.14
bandit==1.9.4
