# Everything needed to boot the Django project: the web service installs only
# this. Deliberately excludes the event-processing deps (Pillow, google-genai)
# so the web image doesn't carry weight it never uses -- see worker.txt.
Django==6.0.4
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
drf_nested_routers==0.95.0
django-extensions==4.1
psycopg[binary]==3.3.3
gunicorn==25.3.0
boto3==1.43.14
redis==7.4.0
celery==5.6.3
# camera.tasks.sweep_readers polls the MediaMTX API over HTTP.
httpx==0.28.1
