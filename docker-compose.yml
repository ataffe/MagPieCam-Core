name: MagPieCam-Core
services:
  magpiecam-postgres:
    image: postgres
    container_name: magpiecam-postgres
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - magpiecam-postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USERNAME} -d ${DATABASE_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - magpiecam-network

  magpiecam-redis:
    image: redis
    container_name: magpiecam-redis
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "6379:6379"
    networks:
      - magpiecam-network

  magpiecam-core:
    build: .
    image: magpiecam-core:0.1.0
    container_name: magpiecam-core
    env_file:
      - .env
    tty: true
    stdin_open: true
    depends_on:
      magpiecam-postgres:
        condition: service_healthy
      scout-moto:
        condition: service_healthy
    ports:
      - "8000:8000"
    networks:
      - magpiecam-network

  # Use only 1 worker because each worker has a copy of the rules model
  # If more workers are needed scale the docker container.
  rules-ml-worker:
    build:
      context: .
      args:
        REQUIREMENTS: requirements/hosted.txt
        # Triton JIT-compiles CUDA kernels at runtime, so the ML image needs a
        # C compiler on board.
        INSTALL_ML_BUILD_TOOLS: "1"
    image: magpiecam-core-worker-ml:0.1.0
    container_name: rules-ml-worker
    command: celery -A magpiecamcore worker -Q events --concurrency=1 -l ${CELERY_ML_WORKER_LOG_LEVEL:-INFO}
    environment:
      PRELOAD_RULES_MODEL: "1"
    env_file:
      - .env
    # Expose the host NVIDIA GPU for model inference. Requires the NVIDIA
    # Container Toolkit on the host (nvidia-ctk runtime configure --runtime=docker).
    gpus: all
    # Persist the multi-GB weights across restarts so init() skips the Backblaze
    # download when the dir is already populated.
    volumes:
      - magpiecam-ml-weights:/app/ml_weights
    tty: true
    stdin_open: true
    depends_on:
      magpiecam-postgres:
        condition: service_healthy
      magpiecam-redis:
        condition: service_healthy
      scout-moto:
        condition: service_healthy
    networks:
      - magpiecam-network

  # Stream sweeping worker (camera.tasks)
  stream-command-worker:
    build:
      context: .
    image: magpiecam-core:0.1.0
    container_name: stream-command-worker
    command: celery -A magpiecamcore worker -Q camera --concurrency=2 -l ${CELERY_STREAMING_COMMAND_WORKER_LOG_LEVEL:-INFO}
    env_file:
      - .env
    tty: true
    stdin_open: true
    depends_on:
      magpiecam-redis:
        condition: service_healthy
    networks:
      - magpiecam-network

  # Beat only schedules; it shares the worker image so there's one build to keep
  # in sync rather than three.
  stream-cleanup-scheduler:
    build:
      context: .
    image: magpiecam-core:0.1.0
    container_name: stream-cleanup-scheduler
    command: celery -A magpiecamcore beat -l ${CELERY_BEAT_LOG_LEVEL:-INFO}
    env_file:
      - .env
    tty: true
    stdin_open: true
    depends_on:
      magpiecam-redis:
        condition: service_healthy
    networks:
      - magpiecam-network

  detection-event-consumer:
    build:
      context: .
    image: magpiecam-core:0.1.0
    container_name: detection-event-consumer
    command: python manage.py consume_events
    # The app-level retry in SQSImageQueueClient covers the queue not existing
    # yet; this is the backstop for everything else -- if it still crashes
    # (retries exhausted, transient network loss, etc.) Docker brings it back
    # instead of it staying dead until someone notices.
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      magpiecam-postgres:
        condition: service_healthy
      magpiecam-redis:
        condition: service_healthy
      scout-moto:
        condition: service_healthy
    networks:
      - magpiecam-network

  # Mocks S3 and SQS for local dev so nothing touches real AWS. Seeds the
  # buckets/queue from .env-docker on every start since moto keeps no state.
  scout-moto:
    build:
      context: ./docker/moto
    image: magpiecam-moto:0.1.0
    container_name: scout-moto
    env_file:
      - .env
    ports:
      - "5001:5001" # These need to match for presigned urls in dev env.
    healthcheck:
      # moto now serves TLS-only (run_server.py passes -c/-k); the cert is
      # self-signed for local dev, so this liveness probe skips verification
      # rather than trusting it.
      test: ["CMD", "python", "-c", "import ssl, urllib.request; urllib.request.urlopen('https://localhost:5001/moto-api/', context=ssl._create_unverified_context())"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - magpiecam-network

  scout-media-mtx:
    image: bluenviron/mediamtx:1
    container_name: scout-media-mtx
    environment:
      MTX_RTSPTRANSPORTS: tcp
      MTX_WEBRTCADDITIONALHOSTS: ${DEV_IP}
    volumes:
      - ./config/mediamtx.yml:/mediamtx.yml:ro
    ports:
      - "8554:8554"
      - "8888:8888"
      - "8889:8889"
      - "9997:9997"
      - "8189:8189/udp"
    networks:
      - magpiecam-network

networks:
  magpiecam-network:
    name: magpiecam-network

volumes:
  magpiecam-postgres-data:
  magpiecam-ml-weights:
